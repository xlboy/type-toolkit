设置对象的可写属性

### 参数

- `O` - (**object**): 要设置可写属性的对象源
- `Keys` - (**extends keyof O**): 要设置可写属性的键

### 返回值

`(object)`: 设置了可写属性的对象

### 例子

```ts
import type tt from 'type-toolkit';

type T1 = tt.O.SetWritable<{ a: 1, b: 2, readonly c: 3 }, 'c'> // { a: 1, b: 2, c: 3 }
```
