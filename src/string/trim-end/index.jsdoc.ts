/**
 * $$target-slot: TrimEnd
 *
 * Remove the spaces at the end of the string
 *
 * @param Str (**string**): The string to be trimmed
 *
 * @returns (**string**): The string without the spaces at the end
 *
 * @example
 * ```ts
 * import type tt from 'type-toolkit'
 *
 * type T0 = tt.String.TrimEnd<'1 1, 3  '> // '1 1, 3'
 * type T1 = tt.String.TrimEnd<'  1  '> // '  1'
 * ```
 */
type Preview6113 = 1;
