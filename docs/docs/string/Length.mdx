
import ParamTable from '@site/src/components/ParamTable';
import ReturnValue from '@site/src/components/ReturnValue';

获取字符串的长度

### Parameters
<ParamTable data={[{"name":"Str","type":"string","description":"The string to get the length of"}]} />

### Return Value
<ReturnValue data={{"type":"number","description":"The length of the string"}} />

### Example

```ts
import type tt from 'type-toolkit';

type T0 = tt.S.Length<'abc'> // 3
type T1 = tt.S.Length<''> // 0
```
