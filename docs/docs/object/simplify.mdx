Flatten computed objects for better preview

### Parameters

- `O` - The object to simplify

### Returns

`(object)`: The simplified object

### Example

```ts
import type tt from 'type-toolkit';

type T0 = tt.O.Simplify<A & B & C> // { ...A, ...B, ...C }
```
