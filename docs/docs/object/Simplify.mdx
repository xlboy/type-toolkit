
import ParamTable from '@site/src/components/ParamTable';
import ReturnValue from '@site/src/components/ReturnValue';

将经过计算处理（交叉、...）的对象进行简化（扁平化），以便预览

### Parameters
<ParamTable data={[{"name":"O","type":"object","description":"The object to simplify"}]} />

### Return Value
<ReturnValue data={{"type":"object","description":"The simplified object"}} />

### Example

```ts
import type tt from 'type-toolkit';

type T0 = tt.O.Simplify<A & B & C> // { ...A, ...B, ...C }
```
