
import ParamTable from '@site/src/components/ParamTable';
import ReturnValue from '@site/src/components/ReturnValue';

删除数组中的最后一个元素

### Parameters
<ParamTable data={[{"name":"Arr","type":"ReadonlyArray<any>","description":"The source of the array to delete the last element from"}]} />

### Return Value
<ReturnValue data={{"type":"any[]","description":"The array without the last element"}} />

### Example

```ts
import type tt from 'type-toolkit';

type T0 = tt.A.Pop<[1, 2, 3]> //  [1, 2]
type T1 = tt.A.Pop<[1, 2, 3, 4]> // [1, 2, 3]
```
